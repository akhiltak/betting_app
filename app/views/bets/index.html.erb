<h1>Listing bets</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Bet amount</th>
      <th>Event</th>
      <th>Outcome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bets.each do |bet| %>
      <tr>
        <td><%= bet.user_id %></td>
        <td><%= bet.bet_amount %></td>
        <td><%= bet.event_id %></td>
        <td><%= bet.outcome_id %></td>
        <td><%= link_to 'Show', bet %></td>
        <td><%= link_to 'Edit', edit_bet_path(bet) %></td>
        <td><%= link_to 'Destroy', bet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bet', new_bet_path %>
